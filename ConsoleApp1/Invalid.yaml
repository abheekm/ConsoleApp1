name: cmd multi line
description: ""
settings:
  vcs-steps: []
  build-steps:
  - key: runner_cmd
    name: one test
    disabled: false
    workDir: ""
    args:
    env:
      APP_ENV: "@{APP_ENV}"
      commands: |-
        echo "Reading environment variable..."
      continueOnError: false
    argsProtected: {}
  artifacts: []
